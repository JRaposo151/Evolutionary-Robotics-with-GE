name: ppo
channels:
  - nvidia
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pytorch=2.3.0
  - torchvision
  - torchaudio
  - pytorch-cuda=12.4
  - gymnasium
  - pybullet
  - matplotlib
  - pandas
  - pip:
      - stable-baselines3==2.5.0
      - cloudpickle
      - tqdm
      - bigtree
      - tensorboard

